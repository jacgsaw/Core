<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="ArxanDialogTheme" parent="ThemeOverlay.MaterialComponents.Dialog.Alert">
        <!-- Fondo del diálogo -->
        <item name="android:windowBackground">@drawable/rounded_dialog_background</item>
        <item name="android:background">@drawable/rounded_dialog_background</item>

        <!-- Colores de texto -->
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorSecondary">@android:color/darker_gray</item>

        <!-- Estilo de los botones -->
        <item name="android:buttonStyle">@style/ArxanDialogButton</item>

        <!-- Animaciones -->
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

    <style name="ArxanDialogButton" parent="Widget.MaterialComponents.Button">
        <item name="android:backgroundTint">#FF0000</item> <!-- Rojo -->
        <item name="android:textColor">@android:color/white</item>
    </style>
    <style name="Base.Theme.Arxan" parent="Theme.MaterialComponents.DayNight.NoActionBar">

    </style>
    <style name="Theme.Arxan" parent="Base.Theme.Arxan"/>
</resources>